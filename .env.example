# 服务端口（默认 8080）
PORT=8080

# 数据目录：SQLite 文件、上传材料、备份均存于此（默认 ./data）
DATA_DIR=./data

# 加密密钥：用于 LLM apiKey 等敏感字段加密（AES-256-GCM），32 字符以上随机字符串
SECRET_KEY=change-me-to-a-random-secret

# 首次启动若无任何用户，用以下账号创建管理员；未设置则随机生成密码打印到日志
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
