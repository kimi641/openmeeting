# 本地部署会议设计排程系统 —— 技术方案与路线图（v5）

> 定位：**开源、可单机部署**的会议全流程管理工具，按会议规模做场景分层。社区版免费（AGPLv3），企业版收费（合规与协同能力）。核心壁垒：数据不出内网，替代 Excel + Word + 邮件的排会方式。

---

## 一、场景分层与产品策略

小、中、大型会议是三个不同的市场，但共享同一个产品内核（议程编排、时间×空间资源、人员角色、材料管理）。策略是**一个内核、场景分层**：

| | 小型（党委会类） | 中型（行业年会/闭门论坛） | 大型（WAIC级） |
|---|---|---|---|
| 典型形态 | 10人左右、几小时、单会议室 | 30–300人、1–3天、多公司参与 | 数千人、3–5天、多展馆、100+展商 |
| 付钱的人 | 单位行政/办公室（政企采购） | 主办方：协会、市场部、公关公司 | 政府/头部主办方，项目招标 |
| 核心痛点 | 流程合规：议题、材料、留痕、保密 | **跨公司协同**：收集议程与嘉宾信息、日程编排、报名通知 | 资源调度：多场馆、展商、票务 |
| 竞争对手 | OA 自带会议模块 | 31会议、百格活动等 SaaS | 定制服务商（项目制生意） |
| 进入难度 | 中（要资质） | **低**（决策快、愿试新工具） | 极高（关系驱动） |
| **产品策略** | 企业版的钱袋：合规留痕、分级审批 | **MVP 锚点 + 社区冷启动土壤** | 暂不做，远期企业版定制方向 |

**为什么 MVP 锚定中型闭门/半闭门会议**（30–100人、跨公司、1–3天）：
- 协会、行业社群、中小企业决策快，不需要过采购流程，是开源项目攒早期用户的理想人群；
- "多公司协同收集议程"是现有 SaaS 做不好、Excel 最痛的点（几十家公司信息靠邮件回收，日程改一版发一圈）；
- 其中有保密要求的闭门会（涉及未发布产品、财务数据）天然流向私有化部署，与小型场景的商业逻辑汇合。

---

## 二、MVP 范围

第一版目标：**把一场中型闭门会的"设计—排程—协同—材料"闭环跑通**。

| 模块 | MVP 内容 | 明确不做（后续版本） |
|---|---|---|
| 会议设计 | 会议创建、多日程（track/session）编排、拖拽排序、议程模板 | 表决投票、纪要生成 |
| 排程 | 时间冲突检测（场地 + 人员双维度）、日历视图 | 复杂资源调度算法 |
| 跨公司协同 | 外单位联系人管理、议程/嘉宾信息收集表（链接填写）、变更通知记录 | 在线报名售票、邮件/短信网关（企业版） |
| 参会人 | 通讯录、角色（主办/嘉宾/汇报/列席）、多公司分组 | — |
| 材料 | 材料上传、与议程项关联、会前打包下载 | 脱敏分发、会后回收（企业版） |
| 场景模板 | 内置"党委会""年会论坛"两套模板，开箱即用 | 模板市场 |
| 数据 | Excel 导入（排期表/通讯录/议程收集表，四层设计见第五节）、全程导出 | — |
| 部署 | 单容器 + 离线安装包，SQLite 内嵌，零依赖运行 | 集群、高可用 |

**杀手锏功能：Excel 一键迁移 + 议程收集链接。** 前者降低试用门槛，后者直接替代"邮件来回收集几十家公司议程"的最痛环节。

---

## 三、技术选型

### 3.1 全栈 TypeScript（已确认）

团队主力语言为 TypeScript，前后端统一技术栈，一人即可维护全链路，开源贡献门槛也最低。

| 层 | 选型 | 理由 |
|---|---|---|
| 运行时/后端框架 | **Node 22 LTS + Hono** | Node 22 是 LTS 长期支持版，生态与稳定性最成熟；Hono 轻量、TS 原生、类型推导到前端 |
| 数据库 | **SQLite**（`better-sqlite3`）+ **Drizzle ORM** | 内嵌零依赖；Drizzle 是 TS 生态里迁移与类型安全最成熟的方案；Node 内置 `node:sqlite` 待其稳定后可平滑切换 |
| 前端 | **React + Vite + TS + Tailwind + shadcn/ui** | 拖拽用 dnd-kit，日程视图用 FullCalendar，Excel 解析用 SheetJS |
| 前后端契约 | Hono RPC / zod 共享 schema | 全栈类型安全，API 改动前后端同步报错 |
| LLM 网关 | OpenAI 兼容协议客户端 | TS 生态成熟，见第五节 |

**已知代价与对策**：
- **放弃单二进制路径**：Node SEA 对原生模块（better-sqlite3）支持差，不作为交付形态；部署统一走容器和离线安装包，把"单容器 5 分钟跑起来"做到极致；
- **不支持龙芯 LoongArch**（Node 无官方支持）：MVP 不管，记入企业版风险清单，真遇到龙芯需求用"容器化 + 龙芯社区移植运行时"评估，或放弃该单；
- Docker 镜像约 150–200MB（Go 方案 <50MB），通过多阶段构建 + node:22-alpine 基础镜像压缩，对可跑 Docker 的内网环境完全够用。

### 3.2 部署形态（三种，全部离线可用）

1. **单容器（主路径）**：`docker run -v ./data:/data meeting-ce:latest`，基于 node:22-alpine 多阶段构建，x86_64 + ARM64（鲲鹏/飞腾）双架构；
2. **离线安装包**：tar.gz 内含镜像包（`docker load` 导入）+ 启动脚本 + 默认配置，适配无法访问镜像仓库的内网；
3. **PM2/systemd 直装（兜底）**：针对完全无 Docker 的老旧内网，提供 Node 运行时便携包 + 一键安装脚本，作为最后手段。

---

## 四、系统架构

```
┌─────────────────────────────────────────────┐
│  浏览器（内网任意终端；外单位通过收集链接填写）  │
└──────────────────┬──────────────────────────┘
                   │ HTTP（可选 HTTPS/国密 TLCP）
┌──────────────────▼──────────────────────────┐
│  Node 22 服务（Hono）                         │
│  ├─ 静态资源伺服（内嵌前端构建产物）            │
│  ├─ REST API（会议/日程/人员/协同/材料/导入）   │
│  ├─ 冲突检测引擎（场地×人员×时间段）            │
│  ├─ 协同收集模块（免登录填写链接 + 提交审核）   │
│  ├─ Excel 解析引擎（规则层，见第五节）          │
│  ├─ LLM 网关（可选，默认关闭，见第五节）        │
│  ├─ 本地认证（账号密码；企业版接 LDAP/AD）     │
│  └─ 审计日志中间件（企业版开启完整留痕）        │
└──────────────────┬──────────────────────────┘
                   │ Drizzle ORM
┌──────────────────▼──────────────────────────┐
│  SQLite 文件（WAL）+ 本地文件存储（会议材料）   │
└─────────────────────────────────────────────┘
```

设计要点：
- **无外部依赖**：不依赖 Redis、消息队列、对象存储，一切内嵌；
- **离线优先**：不请求任何外网资源（字体、统计上报全部不做或默认关闭）；
- **备份即拷贝**：备份 = 复制数据目录，恢复 = 放回去；
- **协同链接可关**：涉及保密时管理员可一键关闭外单位收集入口，纯内网运行；
- **LLM 默认关闭**：未配置模型时所有功能完整可用，AI 只是增强项。

---

## 五、Excel 导入与大模型接入设计

### 5.1 Excel 导入的四层设计（无大模型也能做好）

原则：**不做"理解任意 Excel"，只做三张语义明确的表**（排期表、通讯录、议程收集表），把"理解"从程序转移到用户，程序只做匹配和校验。

1. **收窄问题域**：每张表目标字段固定（排期表 = 时间/议题/汇报人/场地），解析从"理解一张表"降级为"把 N 个列名映射到约 10 个固定字段"；
2. **模板引流**：导入入口旁放"下载标准模板"，行政用户多数会直接填进模板，从源头消化一半以上的格式问题；
3. **字段映射向导**（核心引擎）：
   - 自动识别表头行（扫描前 10 行找关键词命中率最高者），处理合并单元格、多级表头；
   - 列名模糊匹配：内置同义词词典（`姓名/名字/联系人/汇报人/主讲/嘉宾` → 人员），中文会议时间解析规则（"8月22日下午""14:00-15:30""第二天上午"）；
   - 用户确认映射：推荐值 + 置信度，低置信度标黄人工点选；**映射方案可存为预设**，同单位格式稳定，第二次导入一键完成；
4. **预览兜底**：提交前展示解析结果，错位行、解析失败标红，就地改完再入库。

预期效果：模板用户零成本，非模板用户三次点击，规则引擎覆盖 80–90% 的真实表格。

### 5.2 大模型接入架构（预留接口，可选增强）

**设计原则：LLM 是插件，不是依赖。** 未配置时规则引擎全量工作；配置后作为"智能解析"增强。目标客户数据不出内网，云端大模型是禁品，所以架构必须同时支持**本地模型**和**合规批准的云端 API**。

**LLM 网关抽象层**（TS 接口）：

```ts
interface LLMProvider {
  name: string
  chat(req: ChatRequest): Promise<ChatResponse>
}
```

统一走 **OpenAI 兼容协议**（`/v1/chat/completions`），主流厂商几乎全部支持该协议，一个客户端即可覆盖：

| 类别 | 厂商/模型 | 接入方式 |
|---|---|---|
| 国际 | OpenAI (GPT 系列)、Anthropic (Claude)、Google (Gemini) | OpenAI 兼容端点或官方 SDK 适配器 |
| 国内 | DeepSeek、通义千问（阿里百炼）、智谱 GLM、Kimi（月之暗面）、文心一言（百度千帆）、豆包（火山方舟） | 均为 OpenAI 兼容端点，配置 baseURL + API Key 即可 |
| **本地（涉密场景主推）** | Ollama / vLLM / LM Studio 跑 Qwen、DeepSeek 蒸馏版等 | 本地 OpenAI 兼容端点，数据不出域 |

配置方式：管理后台填 `provider / baseURL / apiKey / model` 四项，内置上述厂商的预设模板，用户只填 Key。支持配置多个端点并按场景路由（如解析走本地模型，其他走云端）。

**LLM 应用场景（按优先级）**：

| 场景 | 说明 | 版本归属 |
|---|---|---|
| Excel 智能解析 | 规则引擎置信度低时调用 LLM 做列映射与单元格语义解析 | 企业版 |
| 议程合理性建议 | 检查议程时间分配、嘉宾冲突、议程结构优化建议 | 企业版（后续） |
| 会议纪要辅助 | 基于议程和上传材料生成纪要草稿 | 企业版（后续） |

安全约束：LLM 网关默认关闭；开启后所有外发请求写入审计日志（发了什么、发给哪个端点），满足政企合规审查；支持管理员强制锁定"仅本地端点"。

---

## 六、数据模型草案（核心表）

```sql
meetings          -- 会议：名称、密级、规模场景(小型/中型)、状态、起止日期
tracks            -- 日程线/分会场：会议ID、名称、场地（中型会议多天多线；小型会议默认单线）
sessions          -- 议程项/场次：trackID、序号、标题、起止时间、汇报人/嘉宾、关联材料
venues            -- 场地：名称、容量、设备、可用时段
organizations     -- 参与单位：名称、联系人、对接人（跨公司协同的基础）
participants      -- 人员：姓名、单位ID、职务、联系方式
session_speakers  -- 场次-嘉宾关联：角色(主持/演讲/圆桌)、确认状态
collect_forms     -- 协同收集表：会议ID、字段配置、链接token、截止时间、状态
collect_entries   -- 收集提交：表单ID、单位ID、提交内容(JSON)、审核状态
materials         -- 材料：文件、关联会议/场次、版本、上传人
import_presets    -- Excel 导入映射预设：表类型、列映射方案、创建单位
llm_endpoints     -- LLM 端点配置：provider、baseURL、apiKey(加密存储)、model、启用状态
templates         -- 场景模板：党委会/年会论坛，JSON 结构
users / sessions_auth -- 本地账号与会话
audit_logs        -- 审计日志（企业版全量开启）：谁、何时、对什么、做了什么（含 LLM 外发记录）
```

冲突检测逻辑：同一场地时间段重叠 → 硬冲突；同一人员跨场次重叠 → 软警告。规则写在独立引擎模块，便于企业版扩展（如"涉密会议前后 30 分钟锁定场地"）。

---

## 七、开源治理清单（与商业化直接相关，第一天就要做）

- [ ] **LICENSE**：AGPLv3 全文入库；README 明示"商业授权请联系 xxx"
- [ ] **CLA**：接入 CLA Assistant bot，所有 PR 强制签署（双授权的版权前提）
- [ ] **许可证扫描**：CI 挂 `license-checker`，禁止引入 AGPL 不兼容依赖
- [ ] **CONTRIBUTING.md**：贡献流程、代码规范、CLA 说明
- [ ] **SECURITY.md**：漏洞披露渠道（政企客户会查这个）
- [ ] **社区版/企业版功能对照表**：公开承诺划线规则不随意变更
- [ ] 可选：同时声明木兰许可证（MulanPSL-2.0）兼容，照顾国内政企法务习惯

**社区版 / 企业版划线**：

| 社区版（AGPL，免费） | 企业版（商业授权） |
|---|---|
| 会议/日程/场次编排、冲突检测 | **审计留痕与操作日志**（等保/内控刚需） |
| 跨公司协同收集、参会人管理 | **分级审批流**（涉密会议管控） |
| 材料管理、场景模板 | **LDAP/AD 域集成、细粒度权限** |
| 单机部署、SQLite | **等保合规报表、国密支持、信创适配认证** |
| Excel 规则引擎导入导出 | **LLM 智能解析、议程建议、纪要辅助** |
| — | **材料脱敏分发与会后强制回收、在线报名** |

---

## 八、路线图

| 里程碑 | 周期 | 内容 | 验收标准 |
|---|---|---|---|
| **M1 骨架** | 3–4 周 | 仓库+CI+许可证治理；会议/日程/人员 CRUD；单容器 + 离线安装包构建 | 一台干净内网机器 5 分钟跑起来 |
| **M2 闭环** | 4–6 周 | 冲突检测、日历视图、协同收集链接、材料管理、Excel 四层导入（规则层）、LLM 网关接口预留 | 用真实 Excel 数据完整排一场 3 天、50 人、跨 10 家公司的论坛 |
| **M3 发布** | 2–3 周 | 场景模板、Docker 镜像（x86_64+ARM64）、离线安装包、文档站、v0.1.0 发版 | GitHub 公开，招募 3 个协会/主办方试点 |
| **M4 企业版启动** | M3 之后 | 审计留痕、分级审批、LDAP、LLM 智能解析上线；确定报价与授权文件模板 | 产生第一笔付费（授权或服务） |

---

## 九、关键风险与对策

1. **社区版没人用 → 付费漏斗断**：MVP 体验优先级高于功能数量，Excel 迁移、协同收集链接、部署简易度是生命线。
2. **场景蔓延**：三个场景共享内核，但 UI 和流程差异大；MVP 只做中型模板，小型靠模板兼容，大型坚决不碰。
3. **集成商白嫖**：AGPL + 不提供宽松协议；发现打包销售即谈判授权。
4. **无 Docker 环境的部署摩擦**：放弃单二进制后，老旧内网靠离线安装包和 PM2 直装兜底，这两条路要在 M1 就做出可用版本并纳入验收，不能拖到有客户再补。
5. **龙芯信创缺口**：Node 不支持 LoongArch，遇到龙芯采购需求时单独评估（容器化 + 社区移植运行时），不作为 MVP 约束。
6. **LLM 依赖蔓延**：AI 功能必须永远"拔掉模型也能用"，避免为了炫技把核心流程绑死在模型上；涉密客户很多环境根本没有模型可用。
7. **功能蔓延**：MVP 表格里"不做"一栏和"做"一栏同等重要，每加一个功能先问"Excel 用户会因为这个留下来吗"。
